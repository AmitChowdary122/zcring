---
type: session
date: 2026-08-09
---

# Session — 9 Aug 2026

## What happened

**Ryzen N=8 run dropped.** User challenged the recommendation; the challenge
was correct and dissolved the premise. Full reasoning in
[[decisions/no-ryzen-n8-run]]. Reframed in `README.md`, `CLAUDE.md` and deck
slide 11 from *"unmeasured on adequate hardware"* to *"consumer count is
bounded by core count, on any platform"* — same limitation, strictly stronger
framing. Deck regenerated, 85 KB.

**Huge-page work landed** (commit `67cb601`, built the previous night).
Implementation, fallbacks and the A/B script are in;
[[hypotheses/tlb-hugepages]] has the detail. Two side wins worth more than the
optimisation itself: `--pages=4k` is now explicit in the sweep scripts, and
`ZC_HUGE_REQUIRE` fails rather than falling back.

**The A/B was run** — user executed the three commands and committed
`results/hugepage_ab.csv` (commit `cf27fc1`). Push was rejected pending a
rebase against the Windows deck commit; the rebase command was handed over.
**I have not seen the numbers.**

**`STATUS.md` cleaned up:** fan-out regen marked done, Ryzen N=8 struck with
reasoning so it doesn't get reinstated, and the invented "8–10 Aug abstract
deadline" removed.

**This vault created**, at the user's request, gitignored, Windows-only.

## Open at session end

1. **Read `results/hugepage_ab.csv`.** First thing next session. Check the
   `pages=` column says `hugetlb`, not `thp-advise`
   ([[hypotheses/thp-is-not-hugepages]]). Time-boxed: under a few percent is a
   negative result, record it and stop.
2. Confirm the Ubuntu rebase and push went through.
3. **Form text** — [[submission/stage1-checklist]]. Next substantive work.

## Note to self

Two things this session went to the user rather than being resolved by me, and
both were correct calls: the Ryzen challenge (I was wrong) and the
huge-page result (I cannot fetch — [[traps/i-cannot-fetch]]). The pattern to
keep: when I can't verify, say so and ask, rather than inferring from a stale
tree.
