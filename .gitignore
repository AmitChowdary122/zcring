build/
*.o
*.csv.tmp
perf.data*
.vscode/
.idea/

# results/ is deliberately NOT ignored — raw benchmark data belongs in the
# repo. A performance claim whose raw data is not published is not checkable.

# scratch previews from docs/deck.py rendering — never committed
.deckpreview/

# Private working notes, not part of the project.
vault/
CLAUDE.md
